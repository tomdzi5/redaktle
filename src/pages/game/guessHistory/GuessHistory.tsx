import { StyledCard } from './GuessHistory.styled';

const GuessHistory = () => {
    // redux state
    // komponenty z mui (sortowanie w przyszlosci)
    // scroll
    // flex zalezny od rozmiaru
    return (
        <StyledCard>
            guess history
        </StyledCard>
    )
}

export default GuessHistory;