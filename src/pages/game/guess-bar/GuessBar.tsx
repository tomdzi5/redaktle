import styled from 'styled-components';

const StyledFooter = styled.div`
  position: fixed;
  bottom: 0;
  width: 100%;
`

const GuessBar = () => {
    return (
        <StyledFooter>
            footer
        </StyledFooter>
    )
}

export default GuessBar;